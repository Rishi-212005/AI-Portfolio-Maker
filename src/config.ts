/**
 * Global Configuration for PortGen AI
 */

// Connection base URL for the backend API
// Collaborators can set VITE_API_URL in a root .env file or modify the fallback below.
export const API_URL = import.meta.env.VITE_API_URL || "http://localhost:4000";
